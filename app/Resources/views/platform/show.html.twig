{% extends 'base.html.twig' %}

{% block body %}
    <h1>Platform</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ platform.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ platform.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('platform_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('platform_edit', { 'id': platform.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
